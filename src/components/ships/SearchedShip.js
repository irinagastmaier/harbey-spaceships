import React from 'react';

export default function SearchedShip() {
  return (
    <div style={{ width: '100%' }}>
      {/*    <>
                    {ship.missions.map((mission, i) => (
                       <div key={ship.id}>{mission.name}</div>
                     ))}
                   </>
                 ); */}
    </div>
  );
}
